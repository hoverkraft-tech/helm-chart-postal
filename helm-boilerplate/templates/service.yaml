apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-boilerplate.fullname" . }}
  labels:
    {{- include "helm-boilerplate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "helm-boilerplate.selectorLabels" . | nindent 4 }}
